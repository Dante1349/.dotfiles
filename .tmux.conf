# set panes clickable
set -g mouse on

# change split pane commands
bind / split-window -h
bind - split-window -v
unbind '"'
unbind %

# border color
set -g pane-border-bg color0
set -g pane-border-fg color236
set -g pane-active-border-bg color0
set -g pane-active-border-fg colo82